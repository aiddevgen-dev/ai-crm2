'use client';

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function TenantSettingsPage() {
  return (
    <div className="space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>Account Test123</CardTitle>
        </CardHeader>
        <CardContent>
          {/* Your content */}
        </CardContent>
      </Card>
    </div>
  );
}